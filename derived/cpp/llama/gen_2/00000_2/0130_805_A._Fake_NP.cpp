#include <iostream>

int main() {
    int l, r;
    std::cin >> l >> r;

    if (l == r) {
        std::cout << l << std::endl;
    } else {
        std::cout << 2 << std::endl;
    }

    return 0;
}